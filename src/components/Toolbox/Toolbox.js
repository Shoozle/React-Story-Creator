import React from 'react';
import { ChromePicker } from 'react-color';

const Toolbox = () => {
    return (
        <div>
            <ChromePicker />
        </div>
    );
}

export default Toolbox;